[project]
name = "ffxiv-raid-analyzer"
version = "0.1.0"
description = "A tool to analyze fflogs reports."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyperclip>=1.11.0",
    "python-dotenv>=1.2.2",
    "rich>=15.0.0",
    "typer>=0.26.8",
]
